# ──────────────────────────────────────────────────────────────────────────────
# ClawNo.11 Desktop — 环境变量模板
# 复制此文件为 .env.production，填入配置后重新构建。
# .env.production 已被 .gitignore 屏蔽，永远不会进入代码库。
# ──────────────────────────────────────────────────────────────────────────────

# 当前无需配置任何环境变量。
# 所有敏感数据（API Key 等）均通过应用内加密存储管理。
